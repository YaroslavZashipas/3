public class Main {
    public static void main(String[] args) {

        double a = 3.15;
        double b = 4;
        double c = 3;

        double x = (a + Math.abs(a / (b * c) + b / (a + c)) * Math.log(a * a))
                / (20 * a - Math.sqrt(b * b + c * c));

        double y = a * (Math.atan(c) + Math.exp(b + 1));

        System.out.println(x);
        System.out.println(y);
    }
}
